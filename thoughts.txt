so, everything in our fucking app relies on memory, that means that basically reloading the page (atleast in some aspects of the app) is fatal, that applies to the editing pages not the homepage (bec the homepage has some path logic), so the answer to that is basically to use indexedb or (client side storage) to store the last recieved image from the server, that would be quiet lightweight and convenient. (edit) okay, so what i am doing rn is trying to implement the indexedDB, i want the file to initialize, check if there exists an image inside of the db, if so, we just assign it to the uploaded image in the dom, if not then we shouldnt return any errors. (another update) indexedDB logic very close to work.
